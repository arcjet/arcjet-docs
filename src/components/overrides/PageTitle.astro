---
import TitleByFramework from "@/components/TitleByFramework";
import Default from "@astrojs/starlight/components/PageTitle.astro";

const props = Astro.locals.starlightRoute;
const titleByFramework = props.entry.data.titleByFramework;
---

{titleByFramework && (
<TitleByFramework client:load {...titleByFramework} />
)}

{! titleByFramework && (
<Default>
  <slot />
</Default>
)}