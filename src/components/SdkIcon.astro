---
import { SdkIcon as SdkIconReact } from "@/components/SdkIcon";
import type { ArcjetRouteSdkKey, ArcjetSdkVariantKey } from "@/lib/sdk";

type Props = {
    sdk: ArcjetRouteSdkKey;
    variant?: ArcjetSdkVariantKey;
}

const { sdk, variant } = Astro.props;
---

<SdkIconReact sdk={sdk} variant={variant} />
