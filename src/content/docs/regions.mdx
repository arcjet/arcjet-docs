---
title: "Arcjet Cloud API regions"
description: "Which regions the Arcjet Cloud API is deployed to."
---

import Comments from "/src/components/Comments.astro";

The Arcjet Cloud API minimizes latency by running in more than 300 global
locations, so requests are handled as close as possible to your application.
Arcjet automatically routes each request to the closest, lowest-latency region.

<Comments />
