---
title: "Agent guards"
titleByFramework:
  default: "Agent guards"
  claude-agent-sdk: "Agent guards for Claude Agent SDK"
  claude-managed-agents: "Agent guards for Claude Managed Agents"
  crewai: "Agent guards for CrewAI"
  genkit: "Agent guards for Genkit"
  google-adk: "Agent guards for Google ADK"
  langchain: "Agent guards for LangChain"
  langgraph: "Agent guards for LangGraph"
  mastra: "Agent guards for Mastra"
  openai-agents: "Agent guards for OpenAI Agents"
  strands-agents: "Agent guards for Strands Agents"
  tanstack-ai: "Agent guards for TanStack AI"
  vercel-ai: "Agent guards for Vercel AI SDK"
  vercel-eve: "Agent guards for Vercel Eve"
description: "Quick start guide for guarding an AI agent tool with an Arcjet remote policy in Vercel AI SDK, LangChain, CrewAI, LangGraph, Genkit, Google ADK, OpenAI Agents, Strands Agents, TanStack AI, Mastra, Vercel Eve, Claude Agent SDK, and Claude Managed Agents, in JavaScript and Python."
frameworks:
  - claude-agent-sdk
  - claude-managed-agents
  - crewai
  - genkit
  - google-adk
  - langchain
  - langgraph
  - mastra
  - openai-agents
  - strands-agents
  - tanstack-ai
  - vercel-ai
  - vercel-eve
ajToc:
  - text: "Quick start"
    anchor: "quick-start"
    children:
      - text: 1. Configure the policy
        anchor: "1-configure-the-policy"
      - text: 2. Install Arcjet
        anchor: "2-install-arcjet"
      - text: 3. Set your key
        anchor: "3-set-your-key"
      - text: 4. Wrap the tool
        anchor: "4-wrap-the-tool"
      - text: 5. Try the policy
        anchor: "5-try-the-policy"
      - text: Read the decision, not the absence of an email
        anchor: "read-the-decision-not-the-absence-of-an-email"
  - text: "What next?"
    anchor: "what-next"
  - text: "Get help"
    anchor: "get-help"
---

import { CardGrid } from "@astrojs/starlight/components";
import Comments from "/src/components/Comments.astro";
import SlotByFramework from "@/components/SlotByFramework.astro";
import FrameworkLinks from "@/components/FrameworkLinks.astro";
import FrameworkName from "@/components/FrameworkName";
import { frameworks } from "@/lib/prefs";
import { Link } from "@/components/link";
import WhatIsArcjet from "/src/components/WhatIsArcjet.astro";
import SdkReferenceLinkByFramework from "@/components/SdkReferenceLinkByFramework.astro";

import ClaudeInstall from "@/snippets/guards/quick-start/claude-agent-sdk/Install.mdx";
import ClaudeWrap from "@/snippets/guards/quick-start/claude-agent-sdk/Wrap.mdx";
import ClaudeTry from "@/snippets/guards/quick-start/claude-agent-sdk/Try.mdx";
import ClaudeManagedInstall from "@/snippets/guards/quick-start/claude-managed-agents/Install.mdx";
import ClaudeManagedWrap from "@/snippets/guards/quick-start/claude-managed-agents/Wrap.mdx";
import ClaudeManagedTry from "@/snippets/guards/quick-start/claude-managed-agents/Try.mdx";
import CrewaiInstall from "@/snippets/guards/quick-start/crewai/Install.mdx";
import CrewaiWrap from "@/snippets/guards/quick-start/crewai/Wrap.mdx";
import CrewaiTry from "@/snippets/guards/quick-start/crewai/Try.mdx";
import GenkitInstall from "@/snippets/guards/quick-start/genkit/Install.mdx";
import GenkitWrap from "@/snippets/guards/quick-start/genkit/Wrap.mdx";
import GenkitTry from "@/snippets/guards/quick-start/genkit/Try.mdx";
import GoogleAdkInstall from "@/snippets/guards/quick-start/google-adk/Install.mdx";
import GoogleAdkWrap from "@/snippets/guards/quick-start/google-adk/Wrap.mdx";
import GoogleAdkTry from "@/snippets/guards/quick-start/google-adk/Try.mdx";
import LangchainInstall from "@/snippets/guards/quick-start/langchain/Install.mdx";
import LangchainWrap from "@/snippets/guards/quick-start/langchain/Wrap.mdx";
import LangchainTry from "@/snippets/guards/quick-start/langchain/Try.mdx";
import LanggraphInstall from "@/snippets/guards/quick-start/langgraph/Install.mdx";
import LanggraphWrap from "@/snippets/guards/quick-start/langgraph/Wrap.mdx";
import LanggraphTry from "@/snippets/guards/quick-start/langgraph/Try.mdx";
import MastraInstall from "@/snippets/guards/quick-start/mastra/Install.mdx";
import MastraWrap from "@/snippets/guards/quick-start/mastra/Wrap.mdx";
import MastraTry from "@/snippets/guards/quick-start/mastra/Try.mdx";
import OpenaiAgentsInstall from "@/snippets/guards/quick-start/openai-agents/Install.mdx";
import OpenaiAgentsWrap from "@/snippets/guards/quick-start/openai-agents/Wrap.mdx";
import OpenaiAgentsTry from "@/snippets/guards/quick-start/openai-agents/Try.mdx";
import StrandsInstall from "@/snippets/guards/quick-start/strands-agents/Install.mdx";
import StrandsWrap from "@/snippets/guards/quick-start/strands-agents/Wrap.mdx";
import StrandsTry from "@/snippets/guards/quick-start/strands-agents/Try.mdx";
import TanstackAiInstall from "@/snippets/guards/quick-start/tanstack-ai/Install.mdx";
import TanstackAiWrap from "@/snippets/guards/quick-start/tanstack-ai/Wrap.mdx";
import TanstackAiTry from "@/snippets/guards/quick-start/tanstack-ai/Try.mdx";
import VercelAiInstall from "@/snippets/guards/quick-start/vercel-ai/Install.mdx";
import VercelAiWrap from "@/snippets/guards/quick-start/vercel-ai/Wrap.mdx";
import VercelAiTry from "@/snippets/guards/quick-start/vercel-ai/Try.mdx";
import VercelEveInstall from "@/snippets/guards/quick-start/vercel-eve/Install.mdx";
import VercelEveWrap from "@/snippets/guards/quick-start/vercel-eve/Wrap.mdx";
import VercelEveTry from "@/snippets/guards/quick-start/vercel-eve/Try.mdx";
import DemoPage from "@/snippets/guards/quick-start/shared/DemoPage.mdx";
import EveDemoNote from "@/snippets/guards/quick-start/vercel-eve/DemoNote.mdx";

import ClaudeKey from "@/snippets/guards/quick-start/claude-agent-sdk/Key.mdx";
import ClaudeManagedKey from "@/snippets/guards/quick-start/claude-managed-agents/Key.mdx";
import CrewaiKey from "@/snippets/get-started/crewai/Step2.mdx";
import GenkitKey from "@/snippets/get-started/genkit/Step2.mdx";
import GoogleAdkKey from "@/snippets/get-started/google-adk/Step2.mdx";
import LangchainKey from "@/snippets/guards/quick-start/langchain/Key.mdx";
import LanggraphKey from "@/snippets/get-started/langgraph/Step2.mdx";
import MastraKey from "@/snippets/get-started/mastra/Step2.mdx";
import OpenaiAgentsKey from "@/snippets/guards/quick-start/openai-agents/Key.mdx";
import StrandsKey from "@/snippets/guards/quick-start/strands-agents/Key.mdx";
import TanstackAiKey from "@/snippets/get-started/tanstack-ai/Step2.mdx";
import VercelAiKey from "@/snippets/get-started/vercel-ai/Step2.mdx";
import VercelEveKey from "@/snippets/get-started/vercel-eve/Step2.mdx";

This example gives an AI agent two tools. `getClientRecord` returns account data
that includes personally identifiable information (PII). Arcjet guards
`sendEmail`, so the model can read the record but cannot send its sensitive
fields outside the application. Arcjet checks the model-selected recipient and
generated message body before the email provider runs.

<WhatIsArcjet />

<FrameworkLinks
  exclude={frameworks
    .filter((d) => !frontmatter.frameworks.includes(d.key))
    .map((d) => d.key)}
  title="Choose a framework"
/>

## Quick start

This guide shows you how to guard an agent tool in your
<FrameworkName client:load /> project.

### 1. Configure the policy

In the [Arcjet Console](https://console.arcjet.com), create an Agent Guard
policy with the label `email.sent`. Add these inputs:

| Input                | Exposure | Type        |
| -------------------- | -------- | ----------- |
| `recipient`          | `SERVER` | String      |
| `allowed_recipients` | `SERVER` | String list |
| `body`               | `LOCAL`  | String      |

Then add:

1. A **string-list membership** rule requiring `recipient` to be a member of
   `allowed_recipients`.
2. A **sensitive information** rule on `body` that denies
   `BANK_ACCOUNT` and `ROUTING_NUMBER`.

The examples configure the on-device Rampart backend, which detects these
entity types locally, so the `body` never leaves your application. The first
guarded call loads the Rampart model, so it takes noticeably longer than the
ones after it.

Publish the policy before running the example. Until you do, the label
matches nothing and every guard call comes back `NOT_CONFIGURED`, which
falls through to the SDK rule decision rather than denying. See
<Link.Page href="/guards/reference">Testing and reference</Link.Page> for
the other policy statuses.

Framework wrappers take `action` for the same slug you configured as the
policy label. Direct `guard()` calls still use the field name `label`.

### 2. Install Arcjet

In your project root, install the SDK:

<SlotByFramework>
  <ClaudeInstall slot="claude-agent-sdk" />
  <ClaudeManagedInstall slot="claude-managed-agents" />
  <CrewaiInstall slot="crewai" />
  <GenkitInstall slot="genkit" />
  <GoogleAdkInstall slot="google-adk" />
  <LangchainInstall slot="langchain" />
  <LanggraphInstall slot="langgraph" />
  <MastraInstall slot="mastra" />
  <OpenaiAgentsInstall slot="openai-agents" />
  <StrandsInstall slot="strands-agents" />
  <TanstackAiInstall slot="tanstack-ai" />
  <VercelAiInstall slot="vercel-ai" />
  <VercelEveInstall slot="vercel-eve" />
</SlotByFramework>

### 3. Set your key

[Create a free Arcjet account](https://console.arcjet.com) and follow the
instructions to add a site and get a key.

<SlotByFramework>
  <ClaudeKey slot="claude-agent-sdk" />
  <ClaudeManagedKey slot="claude-managed-agents" />
  <CrewaiKey slot="crewai" />
  <GenkitKey slot="genkit" />
  <GoogleAdkKey slot="google-adk" />
  <LangchainKey slot="langchain" />
  <LanggraphKey slot="langgraph" />
  <MastraKey slot="mastra" />
  <OpenaiAgentsKey slot="openai-agents" />
  <StrandsKey slot="strands-agents" />
  <TanstackAiKey slot="tanstack-ai" />
  <VercelAiKey slot="vercel-ai" />
  <VercelEveKey slot="vercel-eve" />
</SlotByFramework>

### 4. Wrap the tool

Wrap the send-email tool so Arcjet evaluates the remote policy before the email
provider runs.

Guarding one tool only helps if it is the only way to reach the capability.
If the same agent session also exposes an unguarded path to sending mail –
an MCP server it inherited, a built-in tool, a second tool you didn't wrap –
the model can take that path instead. Keep the session's tool surface to
what you wrapped.

<SlotByFramework>
  <ClaudeWrap slot="claude-agent-sdk" />
  <ClaudeManagedWrap slot="claude-managed-agents" />
  <CrewaiWrap slot="crewai" />
  <GenkitWrap slot="genkit" />
  <GoogleAdkWrap slot="google-adk" />
  <LangchainWrap slot="langchain" />
  <LanggraphWrap slot="langgraph" />
  <MastraWrap slot="mastra" />
  <OpenaiAgentsWrap slot="openai-agents" />
  <StrandsWrap slot="strands-agents" />
  <TanstackAiWrap slot="tanstack-ai" />
  <VercelAiWrap slot="vercel-ai" />
  <VercelEveWrap slot="vercel-eve" />
</SlotByFramework>

### 5. Try the policy

Keep identity, allowed recipients, and sensitive records on the server. The
browser sends only the scenario name.

<SlotByFramework>
  <ClaudeTry slot="claude-agent-sdk" />
  <ClaudeManagedTry slot="claude-managed-agents" />
  <CrewaiTry slot="crewai" />
  <GenkitTry slot="genkit" />
  <GoogleAdkTry slot="google-adk" />
  <LangchainTry slot="langchain" />
  <LanggraphTry slot="langgraph" />
  <MastraTry slot="mastra" />
  <OpenaiAgentsTry slot="openai-agents" />
  <StrandsTry slot="strands-agents" />
  <TanstackAiTry slot="tanstack-ai" />
  <VercelAiTry slot="vercel-ai" />
  <VercelEveTry slot="vercel-eve" />
</SlotByFramework>

<SlotByFramework>
  <DemoPage slot="claude-agent-sdk" />
  <DemoPage slot="claude-managed-agents" />
  <DemoPage slot="crewai" />
  <DemoPage slot="genkit" />
  <DemoPage slot="google-adk" />
  <DemoPage slot="langchain" />
  <DemoPage slot="langgraph" />
  <DemoPage slot="mastra" />
  <DemoPage slot="openai-agents" />
  <DemoPage slot="strands-agents" />
  <DemoPage slot="tanstack-ai" />
  <DemoPage slot="vercel-ai" />
  <EveDemoNote slot="vercel-eve" />
</SlotByFramework>

Each scenario demonstrates a different result from the same guarded tool:

- **Allowed recipient:** The recipient is on the allow list and the body has no
  sensitive data, so `sendEmail` reaches the email provider.
- **Blocked recipient:** The model calls the same tool with an external
  address, and the remote membership rule denies the call before the provider
  runs. The model receives the denial result and explains it. This scenario
  needs an adapter that maps `inputs`; on the others the send goes through,
  because the recipient never reaches the policy.
- **Sensitive information:** The recipient is allowed, but the agent first
  calls `getClientRecord` and receives test bank account and routing numbers.
  When the model puts that tool result in the email body, the check denies
  `sendEmail` before the data leaves the application – through the remote
  policy's `body` rule where the adapter maps `inputs`, and through the SDK
  rule where it doesn't.

### Read the decision, not the absence of an email

The samples give the agent a system prompt for a reason. Without one the
model asks a clarifying question, or masks the account numbers itself,
instead of calling `sendEmail` with them. Nothing is sent, no guard call is
made, and no decision is returned.

That is the outcome to watch for, because it looks like the guard worked. A
reader with a broken policy, a wrong label, or a missing key sees "nothing
was sent" and concludes the guard is enforcing. Check the decision in the
[Arcjet dashboard](https://console.arcjet.com) or in your logs. No decision
means the model declined, not that the guard denied.

The same distinction is the reason to guard the tool at all. An unguarded
agent often refuses the sensitive-information scenario on its own, because
sending bank details over email looks wrong to the model. That refusal is
judgement: it is non-deterministic, and a different prompt can talk the
model out of it. The blocked-recipient scenario is the deterministic
contrast, since nothing about an external address looks unsafe to the
model.

## What next?

<CardGrid>
  <Link.Card
    title="Framework integrations"
    href="/guards/framework-integrations"
    description="Helpers, denial envelopes, and fail-closed defaults for every adapter."
  />
  <Link.Card
    title="Remote policies"
    href="/guards/remote-policies"
    description="Labels, actors, typed inputs, and supported policy rules."
  />
  <Link.Card
    title="Capture events"
    href="/guards/capture"
    description="Record that an allowed action happened, without changing the decision."
  />
  <Link.Card
    title="Testing and reference"
    href="/guards/reference"
    description="Test denial and availability behavior and review the API."
  />
  <SdkReferenceLinkByFramework />
</CardGrid>

## Get help

Need help with anything? Email support@arcjet.com to get support from our
engineering team.

<Comments />
