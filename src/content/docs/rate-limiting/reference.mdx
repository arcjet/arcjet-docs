---
title: "Rate limiting reference"
description: "Reference guide for adding Arcjet rate limiting to your Next.js, NestJS, Node.js, Bun, Python, Remix, or SvelteKit app."
frameworks:
  - bun
  - claude-agent-sdk
  - genkit
  - langchain
  - langchain-js
  - langgraph
  - mastra
  - nest-js
  - next-js
  - node-js
  - openai-agents
  - python-fastapi
  - python-flask
  - remix
  - sveltekit
  - vercel-ai
  - vercel-eve
ajToc:
  - text: "Configuration"
    anchor: "configuration"
    children:
      - text: "Fixed window"
        anchor: "fixed-window-rate-limit-options"
      - text: "Sliding window"
        anchor: "sliding-window-rate-limit-options"
      - text: "Token bucket"
        anchor: "token-bucket-rate-limit-options"
  - text: "Identifying users"
    anchor: "identify-users"
  - text: "Guards and routes"
    anchor: "guards-and-routes"
    framework: ["nest-js"]
    children:
      - text: "Global guard"
        anchor: "global-guard"
      - text: "Per route guard"
        anchor: "per-route-guard"
      - text: "Within route"
        anchor: "within-route"
  - text: "Per route versus middleware"
    anchor: "per-route-versus-middleware"
    framework: ["next-js"]
    children:
      - text: "Per route"
        anchor: "per-route"
      - text: "Middleware"
        anchor: "middleware"
      - text: "Avoiding double protection with middleware"
        anchor: "avoid-double-protection-with-middleware"
  - text: "Pages and server actions"
    anchor: "pages-and-server-actions"
    framework: ["next-js"]
  - text: "Loader versus action"
    anchor: "loader-versus-action"
    framework: ["remix"]
    children:
      - text: "Action"
        anchor: "action"
  - text: "Per route versus hooks"
    anchor: "per-route-versus-hooks"
    framework: ["sveltekit"]
    children:
      - text: "Per route"
        anchor: "per-route"
      - text: "Rate limit only on /api/*"
        anchor: "rate-limit-only-on-api"
      - text: "Rate limit on all routes"
        anchor: "rate-limit-on-all-routes"
      - text: "Avoiding double protection with hooks"
        anchor: "avoid-double-protection-with-hooks"
  - text: "Decision"
    anchor: "decision"
    children:
      - text: "Token bucket request"
        anchor: "token-bucket-request"
      - text: "Rate limit headers"
        anchor: "rate-limit-headers"
  - text: "Error handling"
    anchor: "error-handling"
  - text: "Testing"
    anchor: "testing"
  - text: "Examples"
    anchor: "examples"
    framework: ["next-js"]
    children:
      - text: "Rate limit by IP address"
        anchor: "rate-limit-by-ip-address"
      - text: "Rate limit by IP address with custom response"
        anchor: "rate-limit-by-ip-address-with-custom-response"
      - text: "Rate limit by AI tokens"
        anchor: "rate-limit-by-ai-tokens"
      - text: "Rate limit by API key header"
        anchor: "rate-limit-by-api-key-header"
      - text: "Global rate limit"
        anchor: "global-rate-limit"
      - text: "Response based on the path"
        anchor: "response-based-on-the-path"
      - text: "Rewrite or redirect"
        anchor: "rewrite-or-redirect"
      - text: "Wrap existing handler"
        anchor: "wrap-existing-handler"
---

import { Badge, Aside, Tabs, TabItem } from "@astrojs/starlight/components";
import { Link } from "@/components/link";
import SlotByFramework from "@/components/SlotByFramework";
import TextByFramework from "@/components/TextByFramework";
import FrameworkLinks from "@/components/FrameworkLinks";
import { frameworks } from "@/lib/prefs";
import Comments from "/src/components/Comments.astro";

import ByUserIdBun from "@/snippets/rate-limiting/reference/bun/ByUserId.mdx";
import DecisionLogBun from "@/snippets/rate-limiting/reference/bun/DecisionLog.mdx";
import ErrorsBun from "@/snippets/rate-limiting/reference/bun/Errors.mdx";
import FixedWindowBun from "@/snippets/rate-limiting/reference/bun/FixedWindow.mdx";
import HeadersBun from "@/snippets/rate-limiting/reference/bun/Headers.mdx";
import SlidingWindowBun from "@/snippets/rate-limiting/reference/bun/SlidingWindow.mdx";
import TokenBucketBun from "@/snippets/rate-limiting/reference/bun/TokenBucket.mdx";
import TokenBucketRequestBun from "@/snippets/rate-limiting/reference/bun/TokenBucketRequest.mdx";

import ByUserIdNestJs from "@/snippets/rate-limiting/reference/nestjs/ByUserId.mdx";
import DecisionLogNestJs from "@/snippets/rate-limiting/reference/nestjs/DecisionLog.mdx";
import NestJsDecoratorRoutes from "@/snippets/rate-limiting/reference/nestjs/DecoratorRoutes.mdx";
import ErrorsNestJs from "@/snippets/rate-limiting/reference/nestjs/Errors.mdx";
import FixedWindowNestJs from "@/snippets/rate-limiting/reference/nestjs/FixedWindow.mdx";
import SlidingWindowNestJs from "@/snippets/rate-limiting/reference/nestjs/SlidingWindow.mdx";
import TokenBucketNestJs from "@/snippets/rate-limiting/reference/nestjs/TokenBucket.mdx";
import TokenBucketRequestNestJs from "@/snippets/rate-limiting/reference/nestjs/TokenBucketRequest.mdx";
import NestJsGuardDecision from "@/snippets/rate-limiting/reference/nestjs/GuardDecision.mdx";
import HeadersNestJs from "@/snippets/rate-limiting/reference/nestjs/Headers.mdx";

import NextJsPerRouteVsMiddleware from "@/snippets/rate-limiting/reference/nextjs/PerRouteVsMiddleware.mdx";
import ByUserIdNextJs from "@/snippets/rate-limiting/reference/nextjs/ByUserId.mdx";
import DecisionLogNextJs from "@/snippets/rate-limiting/reference/nextjs/DecisionLog.mdx";
import ErrorsNextJs from "@/snippets/rate-limiting/reference/nextjs/Errors.mdx";
import ExamplesNextJs from "@/snippets/rate-limiting/reference/nextjs/Examples.mdx";
import FixedWindowNextJs from "@/snippets/rate-limiting/reference/nextjs/FixedWindow.mdx";
import HeadersNextJs from "@/snippets/rate-limiting/reference/nextjs/Headers.mdx";
import NextJsPagesServerActions from "@/snippets/rate-limiting/reference/nextjs/PagesServerActions.mdx";
import SlidingWindowNextJs from "@/snippets/rate-limiting/reference/nextjs/SlidingWindow.mdx";
import TokenBucketNextJs from "@/snippets/rate-limiting/reference/nextjs/TokenBucket.mdx";
import TokenBucketRequestNextJs from "@/snippets/rate-limiting/reference/nextjs/TokenBucketRequest.mdx";

import ByUserIdNodeJs from "@/snippets/rate-limiting/reference/nodejs/ByUserId.mdx";
import DecisionLogNodeJs from "@/snippets/rate-limiting/reference/nodejs/DecisionLog.mdx";
import ErrorsNodeJs from "@/snippets/rate-limiting/reference/nodejs/Errors.mdx";
import FixedWindowNodeJs from "@/snippets/rate-limiting/reference/nodejs/FixedWindow.mdx";
import HeadersNodeJs from "@/snippets/rate-limiting/reference/nodejs/Headers.mdx";
import SlidingWindowNodeJs from "@/snippets/rate-limiting/reference/nodejs/SlidingWindow.mdx";
import TokenBucketNodeJs from "@/snippets/rate-limiting/reference/nodejs/TokenBucket.mdx";
import TokenBucketRequestNodeJs from "@/snippets/rate-limiting/reference/nodejs/TokenBucketRequest.mdx";

import ByUserIdPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/ByUserId.mdx";
import DecisionLogPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/DecisionLog.mdx";
import ErrorsPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/Errors.mdx";
import FixedWindowPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/FixedWindow.mdx";
import HeadersPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/Headers.mdx";
import SlidingWindowPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/SlidingWindow.mdx";
import TokenBucketPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/TokenBucket.mdx";
import TokenBucketRequestPythonFastapi from "@/snippets/rate-limiting/reference/python-fastapi/TokenBucketRequest.mdx";

import ByUserIdPythonFlask from "@/snippets/rate-limiting/reference/python-flask/ByUserId.mdx";
import DecisionLogPythonFlask from "@/snippets/rate-limiting/reference/python-flask/DecisionLog.mdx";
import ErrorsPythonFlask from "@/snippets/rate-limiting/reference/python-flask/Errors.mdx";
import FixedWindowPythonFlask from "@/snippets/rate-limiting/reference/python-flask/FixedWindow.mdx";
import HeadersPythonFlask from "@/snippets/rate-limiting/reference/python-flask/Headers.mdx";
import SlidingWindowPythonFlask from "@/snippets/rate-limiting/reference/python-flask/SlidingWindow.mdx";
import TokenBucketPythonFlask from "@/snippets/rate-limiting/reference/python-flask/TokenBucket.mdx";
import TokenBucketRequestPythonFlask from "@/snippets/rate-limiting/reference/python-flask/TokenBucketRequest.mdx";

import RemixLoaderVsAction from "@/snippets/rate-limiting/reference/remix/LoaderVsAction.mdx";
import ByUserIdRemix from "@/snippets/rate-limiting/reference/remix/ByUserId.mdx";
import DecisionLogRemix from "@/snippets/rate-limiting/reference/remix/DecisionLog.mdx";
import ErrorsRemix from "@/snippets/rate-limiting/reference/remix/Errors.mdx";
import FixedWindowRemix from "@/snippets/rate-limiting/reference/remix/FixedWindow.mdx";
import HeadersRemix from "@/snippets/rate-limiting/reference/remix/Headers.mdx";
import SlidingWindowRemix from "@/snippets/rate-limiting/reference/remix/SlidingWindow.mdx";
import TokenBucketRemix from "@/snippets/rate-limiting/reference/remix/TokenBucket.mdx";
import TokenBucketRequestRemix from "@/snippets/rate-limiting/reference/remix/TokenBucketRequest.mdx";

import SvelteKitPerRouteVsHooks from "@/snippets/rate-limiting/reference/sveltekit/PerRouteVsHooks.mdx";
import ByUserIdSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/ByUserId.mdx";
import DecisionLogSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/DecisionLog.mdx";
import ErrorsSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/Errors.mdx";
import FixedWindowSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/FixedWindow.mdx";
import HeadersSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/Headers.mdx";
import PerRouteVsHooksSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/PerRouteVsHooks.mdx";
import SlidingWindowSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/SlidingWindow.mdx";
import TokenBucketSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/TokenBucket.mdx";
import TokenBucketRequestSvelteKit from "@/snippets/rate-limiting/reference/sveltekit/TokenBucketRequest.mdx";

import TokenBucketGenkit from "@/snippets/rate-limiting/reference/genkit/TokenBucket.mdx";
import TokenBucketRequestGenkit from "@/snippets/rate-limiting/reference/genkit/TokenBucketRequest.mdx";
import ByUserIdGenkit from "@/snippets/rate-limiting/reference/genkit/ByUserId.mdx";
import ErrorsGenkit from "@/snippets/rate-limiting/reference/genkit/Errors.mdx";
import DecisionLogGenkit from "@/snippets/rate-limiting/reference/genkit/DecisionLog.mdx";
import GuardAlgoGenkit from "@/snippets/rate-limiting/reference/genkit/GuardAlgo.mdx";

import TokenBucketClaudeAgentSdk from "@/snippets/rate-limiting/reference/claude-agent-sdk/TokenBucket.mdx";
import TokenBucketRequestClaudeAgentSdk from "@/snippets/rate-limiting/reference/claude-agent-sdk/TokenBucketRequest.mdx";
import ByUserIdClaudeAgentSdk from "@/snippets/rate-limiting/reference/claude-agent-sdk/ByUserId.mdx";
import ErrorsClaudeAgentSdk from "@/snippets/rate-limiting/reference/claude-agent-sdk/Errors.mdx";
import DecisionLogClaudeAgentSdk from "@/snippets/rate-limiting/reference/claude-agent-sdk/DecisionLog.mdx";
import GuardAlgoClaudeAgentSdk from "@/snippets/rate-limiting/reference/claude-agent-sdk/GuardAlgo.mdx";

import TokenBucketLangchain from "@/snippets/rate-limiting/reference/langchain/TokenBucket.mdx";
import TokenBucketRequestLangchain from "@/snippets/rate-limiting/reference/langchain/TokenBucketRequest.mdx";
import ByUserIdLangchain from "@/snippets/rate-limiting/reference/langchain/ByUserId.mdx";
import ErrorsLangchain from "@/snippets/rate-limiting/reference/langchain/Errors.mdx";
import DecisionLogLangchain from "@/snippets/rate-limiting/reference/langchain/DecisionLog.mdx";
import GuardAlgoLangchain from "@/snippets/rate-limiting/reference/langchain/GuardAlgo.mdx";

import TokenBucketLangchainJs from "@/snippets/rate-limiting/reference/langchain-js/TokenBucket.mdx";
import TokenBucketRequestLangchainJs from "@/snippets/rate-limiting/reference/langchain-js/TokenBucketRequest.mdx";
import ByUserIdLangchainJs from "@/snippets/rate-limiting/reference/langchain-js/ByUserId.mdx";
import ErrorsLangchainJs from "@/snippets/rate-limiting/reference/langchain-js/Errors.mdx";
import DecisionLogLangchainJs from "@/snippets/rate-limiting/reference/langchain-js/DecisionLog.mdx";
import GuardAlgoLangchainJs from "@/snippets/rate-limiting/reference/langchain-js/GuardAlgo.mdx";

import TokenBucketLanggraph from "@/snippets/rate-limiting/reference/langgraph/TokenBucket.mdx";
import TokenBucketRequestLanggraph from "@/snippets/rate-limiting/reference/langgraph/TokenBucketRequest.mdx";
import ByUserIdLanggraph from "@/snippets/rate-limiting/reference/langgraph/ByUserId.mdx";
import ErrorsLanggraph from "@/snippets/rate-limiting/reference/langgraph/Errors.mdx";
import DecisionLogLanggraph from "@/snippets/rate-limiting/reference/langgraph/DecisionLog.mdx";
import GuardAlgoLanggraph from "@/snippets/rate-limiting/reference/langgraph/GuardAlgo.mdx";

import TokenBucketMastra from "@/snippets/rate-limiting/reference/mastra/TokenBucket.mdx";
import TokenBucketRequestMastra from "@/snippets/rate-limiting/reference/mastra/TokenBucketRequest.mdx";
import ByUserIdMastra from "@/snippets/rate-limiting/reference/mastra/ByUserId.mdx";
import ErrorsMastra from "@/snippets/rate-limiting/reference/mastra/Errors.mdx";
import DecisionLogMastra from "@/snippets/rate-limiting/reference/mastra/DecisionLog.mdx";
import GuardAlgoMastra from "@/snippets/rate-limiting/reference/mastra/GuardAlgo.mdx";

import TokenBucketOpenaiAgents from "@/snippets/rate-limiting/reference/openai-agents/TokenBucket.mdx";
import TokenBucketRequestOpenaiAgents from "@/snippets/rate-limiting/reference/openai-agents/TokenBucketRequest.mdx";
import ByUserIdOpenaiAgents from "@/snippets/rate-limiting/reference/openai-agents/ByUserId.mdx";
import ErrorsOpenaiAgents from "@/snippets/rate-limiting/reference/openai-agents/Errors.mdx";
import DecisionLogOpenaiAgents from "@/snippets/rate-limiting/reference/openai-agents/DecisionLog.mdx";
import GuardAlgoOpenaiAgents from "@/snippets/rate-limiting/reference/openai-agents/GuardAlgo.mdx";

import TokenBucketVercelAi from "@/snippets/rate-limiting/reference/vercel-ai/TokenBucket.mdx";
import TokenBucketRequestVercelAi from "@/snippets/rate-limiting/reference/vercel-ai/TokenBucketRequest.mdx";
import ByUserIdVercelAi from "@/snippets/rate-limiting/reference/vercel-ai/ByUserId.mdx";
import ErrorsVercelAi from "@/snippets/rate-limiting/reference/vercel-ai/Errors.mdx";
import DecisionLogVercelAi from "@/snippets/rate-limiting/reference/vercel-ai/DecisionLog.mdx";
import GuardAlgoVercelAi from "@/snippets/rate-limiting/reference/vercel-ai/GuardAlgo.mdx";

import TokenBucketVercelEve from "@/snippets/rate-limiting/reference/vercel-eve/TokenBucket.mdx";
import TokenBucketRequestVercelEve from "@/snippets/rate-limiting/reference/vercel-eve/TokenBucketRequest.mdx";
import ByUserIdVercelEve from "@/snippets/rate-limiting/reference/vercel-eve/ByUserId.mdx";
import ErrorsVercelEve from "@/snippets/rate-limiting/reference/vercel-eve/Errors.mdx";
import DecisionLogVercelEve from "@/snippets/rate-limiting/reference/vercel-eve/DecisionLog.mdx";
import GuardAlgoVercelEve from "@/snippets/rate-limiting/reference/vercel-eve/GuardAlgo.mdx";

Arcjet rate limiting lets you define rules which limit the number of
requests a client can make over a period of time.

<FrameworkLinks
  client:load
  exclude={frameworks
    .filter((d) => !frontmatter.frameworks.includes(d.key))
    .map((d) => d.key)}
  title="Choose a framework"
/>

## Configuration

Each rate limit is configured on an exact path with a set of client
characteristics and algorithm specific options.

### Fixed window rate limit options

Tracks the number of requests made by a client over a fixed time window. Options
are explained in the <Link.Page href="/rate-limiting/configuration">Configuration</Link.Page> documentation. For more details about how the algorithm
works, see the <Link.Page href="/rate-limiting/algorithms#fixed-window">fixed window algorithm description</Link.Page>.

<Tabs syncKey="language">
<TabItem label="JavaScript / TypeScript">
```ts
// Options for fixed window rate limit
// See https://docs.arcjet.com/rate-limiting/configuration
type FixedWindowRateLimitOptions = {
  // "LIVE" will block requests. "DRY_RUN" will log only
  mode?: "LIVE" | "DRY_RUN";
  // How the client is identified. See https://docs.arcjet.com/fingerprints
  characteristics?: string[];
  // Time window the rate limit applies to (e.g. "1h", "60s", or seconds as a number)
  window: string | number;
  // Maximum number of requests allowed in the time window
  max: number;
};
```
</TabItem>
<TabItem label="Python">
```py
# Signature for arcjet.fixed_window
# See https://docs.arcjet.com/rate-limiting/configuration
def fixed_window(
    *,
    # Required. Mode.LIVE blocks requests; Mode.DRY_RUN logs only.
    mode: Mode,
    # Time window in seconds the rate limit applies to (integer only — Python
    # does not accept the "1h"/"60s" duration strings that the JS SDK does).
    window: int,
    # Maximum number of requests allowed in the time window
    max: int,
    # How the client is identified. See https://docs.arcjet.com/fingerprints
    characteristics: Sequence[str] = (),
) -> FixedWindow: ...
```
</TabItem>
</Tabs>

#### Fixed window example

<SlotByFramework client:load>
  <FixedWindowBun slot="bun" />
  <FixedWindowNestJs slot="nest-js" />
  <FixedWindowNextJs slot="next-js" />
  <FixedWindowNodeJs slot="node-js" />
  <FixedWindowPythonFastapi slot="python-fastapi" />
  <FixedWindowPythonFlask slot="python-flask" />
  <FixedWindowRemix slot="remix" />
  <FixedWindowSvelteKit slot="sveltekit" />
  <GuardAlgoClaudeAgentSdk slot="claude-agent-sdk" />
  <GuardAlgoGenkit slot="genkit" />
  <GuardAlgoLangchain slot="langchain" />
  <GuardAlgoLangchainJs slot="langchain-js" />
  <GuardAlgoLanggraph slot="langgraph" />
  <GuardAlgoMastra slot="mastra" />
  <GuardAlgoOpenaiAgents slot="openai-agents" />
  <GuardAlgoVercelAi slot="vercel-ai" />
  <GuardAlgoVercelEve slot="vercel-eve" />
</SlotByFramework>

### Sliding window rate limit options

Tracks the number of requests made by a client over a sliding window so that the
window moves with time. Options are explained in the <Link.Page href="/rate-limiting/configuration">Configuration</Link.Page> documentation. For more
details about how the algorithm works, see the <Link.Page href="/rate-limiting/algorithms#sliding-window">sliding window algorithm description</Link.Page>.

<Tabs syncKey="language">
<TabItem label="JavaScript / TypeScript">
```ts
// Options for sliding window rate limit
// See https://docs.arcjet.com/rate-limiting/configuration
type SlidingWindowRateLimitOptions = {
  // "LIVE" will block requests. "DRY_RUN" will log only
  mode?: "LIVE" | "DRY_RUN";
  // How the client is identified. See https://docs.arcjet.com/fingerprints
  characteristics?: string[];
  // The time interval in seconds for the rate limit
  interval: number;
  // Maximum number of requests allowed over the time interval
  max: number;
};
```
</TabItem>
<TabItem label="Python">
```py
# Signature for arcjet.sliding_window
# See https://docs.arcjet.com/rate-limiting/configuration
def sliding_window(
    *,
    # Required. Mode.LIVE blocks requests; Mode.DRY_RUN logs only.
    mode: Mode,
    # The time interval in seconds for the rate limit
    interval: int,
    # Maximum number of requests allowed over the time interval
    max: int,
    # How the client is identified. See https://docs.arcjet.com/fingerprints
    characteristics: Sequence[str] = (),
) -> SlidingWindow: ...
```
</TabItem>
</Tabs>

#### Sliding window example

<SlotByFramework client:load>
  <SlidingWindowBun slot="bun" />
  <SlidingWindowNestJs slot="nest-js" />
  <SlidingWindowNextJs slot="next-js" />
  <SlidingWindowNodeJs slot="node-js" />
  <SlidingWindowPythonFastapi slot="python-fastapi" />
  <SlidingWindowPythonFlask slot="python-flask" />
  <SlidingWindowRemix slot="remix" />
  <SlidingWindowSvelteKit slot="sveltekit" />
  <GuardAlgoClaudeAgentSdk slot="claude-agent-sdk" />
  <GuardAlgoGenkit slot="genkit" />
  <GuardAlgoLangchain slot="langchain" />
  <GuardAlgoLangchainJs slot="langchain-js" />
  <GuardAlgoLanggraph slot="langgraph" />
  <GuardAlgoMastra slot="mastra" />
  <GuardAlgoOpenaiAgents slot="openai-agents" />
  <GuardAlgoVercelAi slot="vercel-ai" />
  <GuardAlgoVercelEve slot="vercel-eve" />
</SlotByFramework>

### Token bucket rate limit options

Based on a bucket filled with a specific number of tokens. Each request
withdraws a token from the bucket and the bucket is refilled at a fixed rate.
Once the bucket is empty, the client is blocked until the bucket refills.
Options are explained in the <Link.Page href="/rate-limiting/configuration">Configuration</Link.Page> documentation. For more details about how the
algorithm works, see the <Link.Page href="/rate-limiting/algorithms#token-bucket">token bucket algorithm description</Link.Page>.

<Tabs syncKey="language">
<TabItem label="JavaScript / TypeScript">
```ts
// Options for token bucket rate limit
// See https://docs.arcjet.com/rate-limiting/configuration
type TokenBucketRateLimitOptions = {
  // "LIVE" will block requests. "DRY_RUN" will log only
  mode?: "LIVE" | "DRY_RUN";
  // How the client is identified. See https://docs.arcjet.com/fingerprints
  characteristics?: string[];
  // Number of tokens to add to the bucket at each interval
  refillRate: number;
  // The interval in seconds to add tokens to the bucket
  interval: number;
  // The maximum number of tokens the bucket can hold
  capacity: number;
};
```
</TabItem>
<TabItem label="Python">
```py
# Signature for arcjet.token_bucket
# See https://docs.arcjet.com/rate-limiting/configuration
def token_bucket(
    *,
    # Required. Mode.LIVE blocks requests; Mode.DRY_RUN logs only.
    mode: Mode,
    # Number of tokens to add to the bucket at each interval
    refill_rate: int,
    # The interval in seconds to add tokens to the bucket
    interval: int,
    # The maximum number of tokens the bucket can hold
    capacity: int,
    # How the client is identified. See https://docs.arcjet.com/fingerprints
    characteristics: Sequence[str] = (),
) -> TokenBucket: ...
```
</TabItem>
</Tabs>

When using a token bucket rate limit, each request must specify the number of
tokens it wishes to withdraw from the bucket. This is done by passing a
`requested` property to the `protect` function.

#### Token bucket example

For how to specify the number of tokens to request, see the [token bucket
request example](#token-bucket-request).

<SlotByFramework client:load>
  <TokenBucketBun slot="bun" />
  <TokenBucketNestJs slot="nest-js" />
  <TokenBucketNextJs slot="next-js" />
  <TokenBucketNodeJs slot="node-js" />
  <TokenBucketPythonFastapi slot="python-fastapi" />
  <TokenBucketPythonFlask slot="python-flask" />
  <TokenBucketRemix slot="remix" />
  <TokenBucketSvelteKit slot="sveltekit" />
  <TokenBucketClaudeAgentSdk slot="claude-agent-sdk" />
  <TokenBucketGenkit slot="genkit" />
  <TokenBucketLangchain slot="langchain" />
  <TokenBucketLangchainJs slot="langchain-js" />
  <TokenBucketLanggraph slot="langgraph" />
  <TokenBucketMastra slot="mastra" />
  <TokenBucketOpenaiAgents slot="openai-agents" />
  <TokenBucketVercelAi slot="vercel-ai" />
  <TokenBucketVercelEve slot="vercel-eve" />
</SlotByFramework>

The amount of tokens to deduct from the bucket is specified in the `requested`
option as a positive integer when calling the Arcjet `protect` function.

<Tabs syncKey="language">
<TabItem label="JavaScript / TypeScript">
```ts
// Deduct 50 tokens from the bucket
// The value for `requested` must be a positive integer
const decision = await aj.protect(req, { requested: 50 });
```
</TabItem>
<TabItem label="Python">
```py
# Deduct 50 tokens from the bucket
# The value for `requested` must be a positive integer
decision = await aj.protect(request, requested=50)  # use aj.protect(request, ...) for arcjet_sync
```
</TabItem>
</Tabs>

<Aside type="note">The requested amount must be a positive integer.</Aside>

## Identify users

Rate limit rules use `characteristics` to identify the client and apply the
limit across requests. The default is to use the client's IP address. However,
you can specify other
<Link.Page href="/fingerprints">characteristics</Link.Page> such as a user ID
or other metadata from your application.

:::caution
Avoid passing personal information such as an email address as a custom
characteristic. The value is hashed on Arcjet's servers, but it's still a best
practice to avoid sending us sensitive information.
:::

In this example we define a rate limit rule that applies to a specific user ID.
The custom characteristic is `userId` with the value passed as a prop on the
`protect` function. You can use any string for the characteristic name and any
`string`, `number` or `boolean` for the value.

<SlotByFramework client:load>
  <ByUserIdBun slot="bun" />
  <ByUserIdNestJs slot="nest-js" />
  <ByUserIdNextJs slot="next-js" />
  <ByUserIdNodeJs slot="node-js" />
  <ByUserIdPythonFastapi slot="python-fastapi" />
  <ByUserIdPythonFlask slot="python-flask" />
  <ByUserIdRemix slot="remix" />
  <ByUserIdSvelteKit slot="sveltekit" />
  <ByUserIdClaudeAgentSdk slot="claude-agent-sdk" />
  <ByUserIdGenkit slot="genkit" />
  <ByUserIdLangchain slot="langchain" />
  <ByUserIdLangchainJs slot="langchain-js" />
  <ByUserIdLanggraph slot="langgraph" />
  <ByUserIdMastra slot="mastra" />
  <ByUserIdOpenaiAgents slot="openai-agents" />
  <ByUserIdVercelAi slot="vercel-ai" />
  <ByUserIdVercelEve slot="vercel-eve" />
</SlotByFramework>

To identify users with different characteristics, such as IP address for anonymous
users and a user ID for logged in users, you can use `withRule` (JS) or
`with_rule()` (Python) to create augmented clients that use different
characteristics. See the
<Link.Page href="/rate-limiting/configuration#ip-address--user-id">example in the custom characteristics section</Link.Page>.

<SlotByFramework client:load>
  <NestJsDecoratorRoutes slot="nest-js" />
  <NextJsPerRouteVsMiddleware slot="next-js" />
  <NextJsPagesServerActions slot="next-js" />
  <RemixLoaderVsAction slot="remix" />
  <SvelteKitPerRouteVsHooks slot="sveltekit" />
</SlotByFramework>

## Decision

Arcjet provides a single `protect` function that is used to execute your
protection rules. This requires a `RequestEvent` property which is the event
context as passed to the request handler.

<SlotByFramework client:load>
  <NestJsGuardDecision slot="nest-js" />
</SlotByFramework>

This function returns a `Promise` that resolves to an
`ArcjetDecision` object. This contains the following properties:

- `id` (`string`) – The unique ID for the request. This can be used to look up
  the request in the Arcjet dashboard. It is prefixed with `req_` for decisions
  involving the Arcjet cloud API. For decisions taken locally, the prefix is
  `lreq_`.
- `conclusion` (`ArcjetConclusion`) – The final conclusion based on evaluating
  each of the configured rules. If you wish to accept Arcjet's recommended
  action based on the configured rules then you can use this property.
- `reason` (`ArcjetReason`) – An object containing more detailed
  information about the conclusion.
- `results` (`ArcjetRuleResult[]`) – An array of `ArcjetRuleResult` objects
  containing the results of each rule that was executed.
- `ip` (`ArcjetIpDetails`) – An object containing Arcjet's analysis of the
  client IP address. For more information, see the SDK reference.

To check whether a rate limit rule returned a deny conclusion, use
`decision.isDenied()` and `decision.reason.isRateLimit()` (JS) /
`decision.is_denied()` and `decision.reason_v2.type == "RATE_LIMIT"` (Python).

You can iterate through the results and check whether a rate limit was applied:

<Tabs syncKey="language">
<TabItem label="JavaScript / TypeScript">
```ts
for (const result of decision.results) {
  console.log("Rule Result", result);
}
```
</TabItem>
<TabItem label="Python">
```py
for result in decision.results:
    print("Rule Result", result)
```
</TabItem>
</Tabs>

This example logs the full result as well as each rate limit rule:

<SlotByFramework client:load>
  <DecisionLogBun slot="bun" />
  <DecisionLogNestJs slot="nest-js" />
  <DecisionLogNextJs slot="next-js" />
  <DecisionLogNodeJs slot="node-js" />
  <DecisionLogPythonFastapi slot="python-fastapi" />
  <DecisionLogPythonFlask slot="python-flask" />
  <DecisionLogRemix slot="remix" />
  <DecisionLogSvelteKit slot="sveltekit" />
  <DecisionLogClaudeAgentSdk slot="claude-agent-sdk" />
  <DecisionLogGenkit slot="genkit" />
  <DecisionLogLangchain slot="langchain" />
  <DecisionLogLangchainJs slot="langchain-js" />
  <DecisionLogLanggraph slot="langgraph" />
  <DecisionLogMastra slot="mastra" />
  <DecisionLogOpenaiAgents slot="openai-agents" />
  <DecisionLogVercelAi slot="vercel-ai" />
  <DecisionLogVercelEve slot="vercel-eve" />
</SlotByFramework>

### Token bucket request

When using a token bucket rule, pass an additional `requested` prop (a positive
integer) to the `protect` function. This is the number of tokens the client is
requesting to withdraw from the bucket.

<SlotByFramework client:load>
  <TokenBucketRequestBun slot="bun" />
  <TokenBucketRequestNestJs slot="nest-js" />
  <TokenBucketRequestNextJs slot="next-js" />
  <TokenBucketRequestNodeJs slot="node-js" />
  <TokenBucketRequestPythonFastapi slot="python-fastapi" />
  <TokenBucketRequestPythonFlask slot="python-flask" />
  <TokenBucketRequestRemix slot="remix" />
  <TokenBucketRequestSvelteKit slot="sveltekit" />
  <TokenBucketRequestClaudeAgentSdk slot="claude-agent-sdk" />
  <TokenBucketRequestGenkit slot="genkit" />
  <TokenBucketRequestLangchain slot="langchain" />
  <TokenBucketRequestLangchainJs slot="langchain-js" />
  <TokenBucketRequestLanggraph slot="langgraph" />
  <TokenBucketRequestMastra slot="mastra" />
  <TokenBucketRequestOpenaiAgents slot="openai-agents" />
  <TokenBucketRequestVercelAi slot="vercel-ai" />
  <TokenBucketRequestVercelEve slot="vercel-eve" />
</SlotByFramework>

### Rate limit headers

With a rate limit rule enabled, you can access additional metadata in every
Arcjet decision result:

- `max` (`number`): The configured maximum number of requests applied to this
  request.
- `remaining` (`number`): The number of requests remaining before `max` is
  reached within the window.
- `window` (`number`): The total amount of seconds in which requests are
  counted.
- `reset` (`number`): The remaining amount of seconds in the window.

These can be used to return `RateLimit` HTTP headers ([draft
RFC](https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers-08)) to
offer the client more detail.

In JavaScript, use [`setRateLimitHeaders`](https://github.com/arcjet/arcjet-js/tree/main/decorate)
from `@arcjet/decorate`. In Python, use `set_rate_limit_headers` from the
`arcjet` package. Both write `RateLimit` and `RateLimit-Policy` headers from the
decision.

When several rate limit results are present, the tightest remaining budget is
advertised. If two policies share the same `max`, no headers are written.

<SlotByFramework client:load>
  <HeadersBun slot="bun" />
  <HeadersNestJs slot="nest-js" />
  <HeadersNextJs slot="next-js" />
  <HeadersNodeJs slot="node-js" />
  <HeadersPythonFastapi slot="python-fastapi" />
  <HeadersPythonFlask slot="python-flask" />
  <HeadersRemix slot="remix" />
  <HeadersSvelteKit slot="sveltekit" />
</SlotByFramework>

This would result in [draft
RFC](https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers-08)
response headers similar to the following:

```http
...
< RateLimit: limit=10, remaining=5, reset=9
< RateLimit-Policy: 10;w=10
...
```

## Error handling

Arcjet is designed to fail open so that a service issue or misconfiguration does
not block all requests. The SDK also times out and fails open after 2000&nbsp;ms
by default. However, in most cases, the response time is less than 20&nbsp;ms to
30&nbsp;ms.

If there is an error condition when processing the rule, Arcjet returns an
`ERROR` result for that rule and you can check the `message` property on the
rule's error result for more information.

If all other rules that were run returned an `ALLOW` result, then the final
Arcjet conclusion is `ERROR`.

<SlotByFramework client:load>
  <ErrorsBun slot="bun" />
  <ErrorsNestJs slot="nest-js" />
  <ErrorsNextJs slot="next-js" />
  <ErrorsNodeJs slot="node-js" />
  <ErrorsPythonFastapi slot="python-fastapi" />
  <ErrorsPythonFlask slot="python-flask" />
  <ErrorsRemix slot="remix" />
  <ErrorsSvelteKit slot="sveltekit" />
  <ErrorsClaudeAgentSdk slot="claude-agent-sdk" />
  <ErrorsGenkit slot="genkit" />
  <ErrorsLangchain slot="langchain" />
  <ErrorsLangchainJs slot="langchain-js" />
  <ErrorsLanggraph slot="langgraph" />
  <ErrorsMastra slot="mastra" />
  <ErrorsOpenaiAgents slot="openai-agents" />
  <ErrorsVercelAi slot="vercel-ai" />
  <ErrorsVercelEve slot="vercel-eve" />
</SlotByFramework>

## Testing

Arcjet runs the same in any environment, including locally and in CI. You can
use the `mode` set to `DRY_RUN` to log the results of rule execution without
blocking any requests.

We have an example test framework you can use to automatically test your rules.
Arcjet can also be triggered based using a sample of your traffic.

For details, see the <Link.Page href="/testing">Testing</Link.Page> section of
the docs.

<SlotByFramework client:load>
  <ExamplesNextJs slot="next-js" />
</SlotByFramework>

<Comments />
