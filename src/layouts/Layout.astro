---
import { main as sidebar } from "@/lib/sidebars";
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";

const { frontmatter, headings } = Astro.props;
---

<StarlightPage frontmatter={frontmatter} headings={headings} sidebar={sidebar}>
  <slot />
</StarlightPage>