import { Code } from "@astrojs/starlight/components";
import SelectableContent from "@/components/SelectableContent";
import Step3Js from "./Step3.js?raw";

Arcjet protection runs on HTTP requests.
You can add Arcjet to your Node server to protect routes.

<SelectableContent client:load frameworkSwitcher syncKey="language">
  <Fragment slotIdx="1" slot="JS">

    Create an `index.js` file with the following code:

    <Code code={Step3Js} lang="js" title="index.js" />

  </Fragment>
  <Fragment slotIdx="2" slot="TS">

    Create an `index.ts` file with the following code:

    <Code code={Step3Js} lang="ts" title="index.ts" />

  </Fragment>
</SelectableContent>
