import Step3 from "./Step3.py?raw";
import { Code } from "@astrojs/starlight/components";

Arcjet protection runs on HTTP requests. You can add Arcjet to your FastAPI
app to protect routes.

Create a new file at `main.py` with the contents:

<Code code={Step3} lang="py" title="main.py" mark={["ARCJET_KEY"]} />
