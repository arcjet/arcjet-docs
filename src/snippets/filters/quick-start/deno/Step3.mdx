import { Code } from "@astrojs/starlight/components";
import Step3Ts from "./Step3.ts?raw";

Arcjet protection runs on HTTP requests.
You can add Arcjet to your Deno server to protect routes.

Create an `index.ts` file with the following code:

<Code code={Step3Ts} lang="ts" title="index.ts" />
