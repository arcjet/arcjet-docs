### 4. Start server

Start your Deno server:

```sh
deno run --allow-env --allow-net --allow-read --env-file --watch index.ts
```

You can also omit the `--allow*` flags so that Deno will prompt for the needed
permissions.
