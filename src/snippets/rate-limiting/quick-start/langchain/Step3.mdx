import Step from "./Step3.py?raw";
import { Code } from "@astrojs/starlight/components";

Wrap a tool so each call withdraws tokens from a bucket. The bucket holds 10
tokens and refills 5 tokens every 10 seconds. This call withdraws 5 tokens.

<Code code={Step} lang="py" title="agent.py" />
