In your project root, install the Arcjet Python SDK along with Flask:

```sh
uv add arcjet flask
```

If you don't use [uv](https://docs.astral.sh/uv/), `pip install arcjet flask`
(or your preferred package manager's equivalent) works too.
