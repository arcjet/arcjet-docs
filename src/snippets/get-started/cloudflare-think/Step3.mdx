import Step from "./Step3.ts?raw";
import { Code } from "@astrojs/starlight/components";
import GuardPolicy from "../shared/GuardPolicy.mdx";

<GuardPolicy />

Create a client, screen inbound text, and assign
`guardHooks().beforeToolCall` on the Think subclass. `Env` comes
from `npx wrangler types`. The snippet declares an empty `Env`
interface so the file type-checks before you run that command:

<Code code={Step} lang="ts" title="agent.ts" />
