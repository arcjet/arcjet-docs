import Step from "./Step3.py?raw";
import { Code } from "@astrojs/starlight/components";
import GuardPolicy from "../shared/GuardPolicy.mdx";

<GuardPolicy />

Then create a client and register the hook so Arcjet evaluates the policy
before the tool runs:

<Code code={Step} lang="py" title="agent.py" />
