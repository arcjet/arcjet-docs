- Node.js 22 or later.
- CommonJS is not supported. Arcjet is ESM only.
- `langchain` `>=1 <2` and `@langchain/core` `>=1 <2`.
- Until `@arcjet/guard/langchain/v1` is published, the import is not in
  the published `@arcjet/guard` package on npm. `@arcjet/guard@1.10.0`
  does not export `./langchain/v1`. TODO: pin the adapter SHA once
  Runtime has a branch.
