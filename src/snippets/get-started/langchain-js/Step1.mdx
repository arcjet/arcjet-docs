import SelectableContent from "@/components/SelectableContent";

Until `@arcjet/guard/langchain/v1` is published, the import is not in
the published `@arcjet/guard` package on npm. `@arcjet/guard@1.10.0`
does not export `./langchain/v1`. TODO: pin the adapter SHA once
Runtime has a branch.

{/* prettier-ignore */}
<SelectableContent client:load syncKey="packageManager">
<div slot="npm" slotIdx="1">

```sh
npm i @arcjet/guard langchain @langchain/core
```

</div>
<div slot="pnpm" slotIdx="2">

```sh
pnpm add @arcjet/guard langchain @langchain/core
```

</div>
<div slot="yarn" slotIdx="3">

```sh
yarn add @arcjet/guard langchain @langchain/core
```

</div>
</SelectableContent>
