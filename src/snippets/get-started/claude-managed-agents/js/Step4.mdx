```shell
npx tsx --env-file .env.local agent.ts
```

Pass the Anthropic session `id` from `sessions.create`. Don't mint an
ID. Denied prompts and custom tool calls appear in your
[Arcjet dashboard](https://console.arcjet.com) in real time.
