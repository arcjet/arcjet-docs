- Node.js 22 or later.
- CommonJS is not supported. Arcjet is ESM only.
- `@anthropic-ai/sdk` (`>=0.86.0 <1`) is an optional peer of
  `@arcjet/guard`. The peer is `@anthropic-ai/sdk`, not
  `@anthropic-ai/claude-agent-sdk`.
