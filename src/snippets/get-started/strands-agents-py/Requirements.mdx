- CPython 3.10 or later.
- `arcjet[strands-agents]` is an extra. The extra pulls the
  Strands Agents SDK. A peer version floor is not published yet.
- The Strands Agents Python adapter isn't in `arcjet` 1.0.0.
  Until it is published, pin `arcjet` to `ADAPTER_SHA`.
