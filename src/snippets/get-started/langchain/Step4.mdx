import { Link } from "@/components/link";

## 4. Start app

```shell
python -c "import asyncio; from agent import run_agent; asyncio.run(run_agent('user123', 'Look up order 1001'))"
```

Then invoke the agent. Denied prompts and tool calls appear in your
[Arcjet dashboard](https://app.arcjet.com) in real time.

For helper options, denial payloads, and correlation, see the
<Link.Page href="/guards/langchain">LangChain agent guard</Link.Page>.
