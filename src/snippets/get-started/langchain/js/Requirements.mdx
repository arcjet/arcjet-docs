- Node.js 22 or later.
- CommonJS is not supported. Arcjet is ESM only.
- `langchain` `>=1.2.0 <2` and `@langchain/core` `>=1 <2`. This
  adapter does not require `@langchain/langgraph`.
- A LangChain provider package for the model you use. This example uses
  `@langchain/openai`.
- `@arcjet/guard` 1.11.0 or later.
