import Step from "./Step3.ts?raw";
import { Code } from "@astrojs/starlight/components";

Then create a client, wrap the tool, and pass `guardMiddleware` on
`createAgent({ middleware })`:

<Code code={Step} lang="ts" title="agent.ts" />
