import Step from "./Step3.go?raw";
import { Code } from "@astrojs/starlight/components";
import GuardPolicy from "../shared/GuardPolicy.mdx";

<GuardPolicy />

Then create a client and wrap the tool with `GuardTool`, so Arcjet evaluates
the policy before the function runs:

<Code code={Step} lang="go" title="agent.go" />
