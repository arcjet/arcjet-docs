Set your environment variables. The Go SDK does not load `.env` files, so
export them in the process or use the project's existing loader:

```ini title=".env"
# Export your Arcjet API key from https://console.arcjet.com
ARCJET_KEY=ajkey_yourkey
ANTHROPIC_API_KEY=sk-yourkey
```
