import Step from "./Step3.py?raw";
import { Code } from "@astrojs/starlight/components";

Create a client, screen inbound text, and pass `guard_plugin`
first on the Runner `plugins` list. Assign `guard_tool` on
`LlmAgent(before_tool_callback=...)` when you hold the agent
instead of the Runner:

<Code code={Step} lang="py" title="agent.py" />
