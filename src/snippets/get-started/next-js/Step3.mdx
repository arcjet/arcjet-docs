import ChatRouteTS from "./ChatRoute.ts?raw";
import ChatRouteJS from "./ChatRoute.js?raw";
import ChatUITSX from "/src/snippets/ai-protection/ChatUI.tsx?raw";
import { Code } from "@astrojs/starlight/components";
import SelectableContent from "@/components/SelectableContent";

This example uses the [Vercel AI SDK](https://sdk.vercel.ai/). Install it along with an AI provider:

{/* prettier-ignore */}
<SelectableContent client:load syncKey="packageManager">
<div slot="npm" slotIdx="1">
```sh
npm install ai @ai-sdk/openai
```
</div>
<div slot="pnpm" slotIdx="2">
```sh
pnpm add ai @ai-sdk/openai
```
</div>
<div slot="yarn" slotIdx="3">
```sh
yarn add ai @ai-sdk/openai
```
</div>
</SelectableContent>

<SelectableContent client:load syncKey="language">
<div slot="TS (App)" slotIdx="1">
Create a new API route at `/app/api/chat/route.ts`:

<Code
  code={ChatRouteTS}
  lang="ts"
  title="/app/api/chat/route.ts"
/>
</div>
<div slot="JS (App)" slotIdx="3">
Create a new API route at `/app/api/chat/route.js`:

<Code
  code={ChatRouteJS}
  lang="js"
  title="/app/api/chat/route.js"
/>
</div>
</SelectableContent>

And create a new page at `/app/page.tsx`:

<Code
  code={ChatUITSX}
  lang="ts"
  title="/app/page.tsx"
/>