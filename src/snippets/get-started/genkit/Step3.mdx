import Step from "./Step3.ts?raw";
import { Code } from "@astrojs/starlight/components";
import GuardPolicy from "../shared/GuardPolicy.mdx";

<GuardPolicy />

Then create a client and wrap the tool so Arcjet evaluates the policy before
it runs:

<Code code={Step} lang="ts" title="agent.ts" />
