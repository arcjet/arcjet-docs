import Step from "./Step3.py?raw";
import { Code } from "@astrojs/starlight/components";

Then create a client, wrap the tool, and pass `guard_hooks` on
`Agent(hooks=[...])`:

<Code code={Step} lang="py" title="agent.py" />
