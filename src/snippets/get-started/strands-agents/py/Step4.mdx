```shell
python -c "from agent import run_agent; print(run_agent('user123', ['1001'], 'Look up order 1001'))"
```

Pass the caller-owned `session_id` that you already have. Don't mint
one. Denied tool calls appear in your
[Arcjet dashboard](https://console.arcjet.com) in real time.
