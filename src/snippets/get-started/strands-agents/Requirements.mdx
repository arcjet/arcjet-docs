- Node.js 22 or later.
- CommonJS is not supported. Arcjet is ESM only.
- `@strands-agents/sdk` `>=1.1.0 <2`.
- Until `@arcjet/guard/strands-agents/v1` is published, this export
  is not in the published `@arcjet/guard` package on npm. Install
  from git once an adapter SHA is provided. There is no adapter SHA
  yet.
