import SelectableContent from "@/components/SelectableContent";

Until `@arcjet/guard/strands-agents/v1` is published, this export is
not in the published `@arcjet/guard` package on npm. The module is
unpublished and will be installed from git once an adapter SHA is
provided. There is no adapter SHA yet. Replace `ADAPTER_SHA` in
`github:arcjet/arcjet-js#ADAPTER_SHA` when one is provided. Do not
invent a commit hash.

{/* prettier-ignore */}
<SelectableContent client:load syncKey="packageManager">
<div slot="npm" slotIdx="1">

```sh
npm i @arcjet/guard @strands-agents/sdk
```

</div>
<div slot="pnpm" slotIdx="2">

```sh
pnpm add @arcjet/guard @strands-agents/sdk
```

</div>
<div slot="yarn" slotIdx="3">

```sh
yarn add @arcjet/guard @strands-agents/sdk
```

</div>
</SelectableContent>
