- Node.js 22 or later.
- CommonJS is not supported. Arcjet is ESM only.
- `@tanstack/ai` `>=0.8.0 <1`.
- The TanStack AI adapter is not in `@arcjet/guard` 1.11.0. Until it
  is published, replace `ADAPTER_SHA` with the SHA when Runtime
  provides one.
