- Node.js 22 or later.
- CommonJS is not supported. Arcjet is ESM only.
- `@tanstack/ai` `>=0.8.0 <1`. TanStack AI 0.25.0 and later infer
  `chat({ middleware })` from a middleware registry, which rejects
  `guardMiddleware` at compile time even though it runs – cast it, or stay on
  0.20.x, until the adapter registers itself.
