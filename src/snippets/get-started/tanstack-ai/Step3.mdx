import Step from "./Step3.ts?raw";
import { Code } from "@astrojs/starlight/components";
import GuardPolicy from "../shared/GuardPolicy.mdx";

<GuardPolicy />

Then create a client and pass `guardMiddleware` on `chat({ middleware })`, so
Arcjet evaluates the policy before a tool runs:

<Code code={Step} lang="ts" title="agent.ts" />
