import SelectableContent from "@/components/SelectableContent";

The TanStack AI adapter is not in the published `@arcjet/guard`
1.11.0 package. Until it is published, install from the adapter
commit. Replace `ADAPTER_SHA` with the SHA when Runtime provides
one.

{/* prettier-ignore */}
<SelectableContent client:load syncKey="packageManager">
<div slot="npm" slotIdx="1">

```sh
npm i @arcjet/guard@ADAPTER_SHA @tanstack/ai
```

</div>
<div slot="pnpm" slotIdx="2">

```sh
pnpm add @arcjet/guard@ADAPTER_SHA @tanstack/ai
```

</div>
<div slot="yarn" slotIdx="3">

```sh
yarn add @arcjet/guard@ADAPTER_SHA @tanstack/ai
```

</div>
</SelectableContent>
