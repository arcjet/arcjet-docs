```shell
python -c "import asyncio; from agent import run_agent; asyncio.run(run_agent('conversation-123', 'Look up order 1001'))"
```

Pass the session ID that you already have. Don't mint one. Denied prompts and
tool calls appear in your
[Arcjet dashboard](https://console.arcjet.com) in real time.
