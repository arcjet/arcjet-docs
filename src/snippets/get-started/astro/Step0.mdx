import { Code } from "@astrojs/starlight/components";
import SelectableContent from "@/components/SelectableContent";
import Step1JS from "./Step1.js?raw";

Create an Astro project:

{/* prettier-ignore */}
<SelectableContent client:load syncKey="packageManager" frameworkSwitcher>
<div slot="npm" slotIdx="1">

```sh
npm create astro@latest
```

</div>
<div slot="pnpm" slotIdx="2">

```sh
pnpm create astro@latest
```

</div>
<div slot="yarn" slotIdx="3">

```sh
yarn create astro@latest
```

</div>
</SelectableContent>

Next enable on-demand rendering. Here we've chosen the node adapter:

{/* prettier-ignore */}
<SelectableContent client:load syncKey="packageManager" frameworkSwitcher>
<div slot="npm" slotIdx="1">

```sh
npx astro add node
```

</div>
<div slot="pnpm" slotIdx="2">

```sh
pnpm astro add node
```

</div>
<div slot="yarn" slotIdx="3">

```sh
yarn astro add node
```

</div>
</SelectableContent>

For detailed instructions refer to the [Astro installation docs](https://docs.astro.build/en/install-and-setup/)
and the [Astro on-demand rendering docs](https://docs.astro.build/en/guides/on-demand-rendering/).
