```py
detect_bot(
    mode=Mode.LIVE,
    deny=[
        "ARCJET_SIGNALS",  # Block requests that fail signals detection
        BotCategory.AI,    # Block AI scrapers
        # All other bots are allowed
    ],
)
```
