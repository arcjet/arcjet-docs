import Step from "./agent.ts?raw";
import { Code } from "@astrojs/starlight/components";
import ActionAndRules from "../shared/ActionAndRules.mdx";

<ActionAndRules />

Create one client and pass `guardPlugin` first on the Runner
`plugins` list. There is no `guardTool`:

<Code code={Step} lang="ts" title="agent.ts" />
