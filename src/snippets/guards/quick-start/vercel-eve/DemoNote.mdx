Eve agents receive channel messages rather than an HTTP route. Register the
wrapped tools on the agent, then send each scenario as a message. Keep
identity, allowed recipients, and the client record on the server.
