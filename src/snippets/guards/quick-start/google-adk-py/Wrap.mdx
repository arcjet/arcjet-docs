import Step from "./Wrap.py?raw";
import { Code } from "@astrojs/starlight/components";
import ActionAndRules from "../shared/ActionAndRules.mdx";

<ActionAndRules />

Create one client and pass `guard_plugin` first on the Runner
`plugins` list. `guard_tool` is the `LlmAgent` callback if you
hold the agent instead of the Runner:

<Code code={Step} lang="py" title="agent.py" />
