import Step from "./Try.ts?raw";
import { Code } from "@astrojs/starlight/components";

Expose a small server endpoint. The browser sends only the scenario name. Mint one UUID session ID per run:

<Code code={Step} lang="ts" title="app/api/agent/route.ts" />
