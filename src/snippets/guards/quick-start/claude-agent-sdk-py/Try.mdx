import Step from "./Try.py?raw";
import { Code } from "@astrojs/starlight/components";

Expose a small server endpoint. The browser sends only the scenario name. Pass the caller-owned UUID `session_id` that you already have. Don't mint one:

<Code code={Step} lang="py" title="app.py" />
