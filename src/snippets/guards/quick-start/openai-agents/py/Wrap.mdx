import Step from "./agent.py?raw";
import { Code } from "@astrojs/starlight/components";
import AcceptsInputs from "../../shared/AcceptsInputs.mdx";

<AcceptsInputs />

Create one client and wrap `send_email` with `guard_tool`:

<Code code={Step} lang="py" title="agent.py" />
