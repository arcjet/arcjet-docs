import Step from "./Try.ts?raw";
import { Code } from "@astrojs/starlight/components";

Expose a small server endpoint that compiles the graph and runs one scenario. The browser sends only the scenario name:

<Code code={Step} lang="ts" title="app/api/agent/route.ts" />
