import Step from "./Try.ts?raw";
import { Code } from "@astrojs/starlight/components";

Add the wrapped tools to your `StateGraph` `ToolNode`, then expose a small server endpoint. The browser sends only the scenario name:

<Code code={Step} lang="ts" title="app/api/agent/route.ts" />
