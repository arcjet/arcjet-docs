import Step from "./Wrap.ts?raw";
import { Code } from "@astrojs/starlight/components";
import ActionAndRules from "../../shared/ActionAndRules.mdx";

<ActionAndRules />

Create one client, wrap `send_email` with `guardCustomTool`, and handle
`agent.custom_tool_use` in your session loop:

<Code code={Step} lang="ts" title="agent.ts" />
