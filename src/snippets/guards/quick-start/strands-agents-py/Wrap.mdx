import Step from "./Wrap.py?raw";
import { Code } from "@astrojs/starlight/components";
import ActionAndRules from "../shared/ActionAndRules.mdx";

<ActionAndRules />

Create one client, wrap `send_email` with `guard_tool`, and pass
`guard_hooks` on `Agent(hooks=[...])`:

<Code code={Step} lang="py" title="agent.py" />
